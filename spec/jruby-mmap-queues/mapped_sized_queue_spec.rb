require "spec_helper"
require "mapped_sized_queue"

describe Mmap::MappedSizedQueue do
  before(:all) do
  end

  after(:all) do
  end

  after(:each) do
  end

  it "should work" do
    expect(true).to eq(true)
  end

end
